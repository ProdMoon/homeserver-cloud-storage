# Direction for Homeserver Cloud Storage Project

## Architecture

- This project is organized as a monorepo using a pnpm workspace.
- Frontend app is located in `apps/web` and is named `@pi-home-drive/web`.
- Backend app is located in `apps/server` and is named `@pi-home-drive/server`.
